[build]
  command = "npm ci --no-audit && npm run generate && echo '--- LS .output/public ---' && ls -la .output/public"
  publish = ".output/public"

[build.environment]
  NODE_VERSION = "20"
  NPM_CONFIG_AUDIT = "false"
